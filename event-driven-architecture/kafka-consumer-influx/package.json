{
  "name": "kafka-consumer-influx",
  "version": "1.0.0",
  "description": "Kafka consumer — reads temperature data from partition 0 and writes to InfluxDB",
  "main": "index.js",
  "scripts": {
    "start": "nodemon -L index.js"
  },
  "dependencies": {
    "@influxdata/influxdb-client": "^1.35.0",
    "express": "^4.21.0",
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
