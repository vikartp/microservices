{
  "name": "kafka-consumer-timescale",
  "version": "1.0.0",
  "description": "Kafka consumer — reads server metrics from partition 1 and writes to TimescaleDB",
  "main": "index.js",
  "scripts": {
    "start": "nodemon -L index.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "kafkajs": "^2.2.4",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
